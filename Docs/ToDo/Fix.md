Fix Rclone mount Hub, fix test connection not testing currently active connection. if local connection doesnt work and it chooses tailscale make sure tailscale ip works not just use it. also when it says mounted test that it actually mounts and the user can see mount, check if drive letter if availble. make sure auto tailscale/local ip detection works with how rclone says its uses local ip and not the tailscale one. make sure logs label if mounted filesystem cant be seen and why currently is always successful. on network change have it recheck which ip to use local or not. fix sometuimes having to remout to see new files i added remotely to the mounted drive, even if i refresh or go up a folder and go back in it doesnt show updated files, i have to unmount and remount to see them